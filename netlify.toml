[build]
  command = "bun run build"
  publish = "dist"
  SECRETS_SCAN_OMIT_KEYS = "SUPABASE_URL,VITE_SUPABASE_URL"
  SECRETS_SCAN_OMIT_PATHS = ".lovable/mcp/manifest.json"

[build.environment]
  NITRO_PRESET = "netlify"
